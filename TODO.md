- [ ] Implement a high-frequency domain vocabulary extension during the Python training phase to mitigate token fragmentation. Update the tokenizer to register the top ~200 recurring domains (e.g., common contacts and persistent spam reflectors) as atomic custom tokens (e.g., `[DOM_github.com]`). This will shrink the model's footprint, optimize the tight 512-token budget for the Raspberry Pi, and protect the network from wasting attention cycles reconstructing split domain strings, while letting one-off domains gracefully fall back to default subword tokenization.
- [ ] [BLOCKER: Wait for SPF-tagged corpus collection] Implement SPF (Sender Policy Framework) metadata feature parsing and ingestion as designed in [docs/feature_spf.md](docs/feature_spf.md) (wait until newly collected mail has enough SPF headers in both ham/spam folders to train effectively).
